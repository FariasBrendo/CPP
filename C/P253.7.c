#include <stdio.h>
main(){
int x, y, contador = 1, produto;

while (contador < 11){
    printf("Digite o valor de x e y respectivamente: \n");
    scanf("%d%d",&x, &y);
    printf("O produto de x e y eh: %d\n", produto = x * y);
    contador++;

}

return 0;
}
